enum MyEnum {
    ENUM1
    ENUM2
}

object Obj {
    a: Str
    b: MyEnum
}

main() {
    obj = Obj(a = "odi")
    obj.b := ENUM1
    if obj.b {
        ENUM1: {print(1)}
        ENUM2: {print(2)}
    }
}
