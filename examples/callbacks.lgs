main() {
    funcVar = (x: Int, y: Int): Void -> {
        print(x + y)
    }
    funcVar = (x: Int, y: Int): Void -> print(x + y)
    funcVar = (x: Int, y: Int) -> print(x + y)
    funcVar = (x, y) -> print(x + y)
    funcVar = x -> print(x) // only with single param
}
