object Ob {
    func(x: Int): Int {
        return x * 2
    }
}

main() {
    arr = [1, 2, 3, 4]
    ob = Ob{}
    a = arr.map(ob.func).filter(it => it > 3)
    for e in a {
        print(e)
    }
}
